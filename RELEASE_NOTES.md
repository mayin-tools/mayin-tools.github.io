# v1.4.0

- 250 个 `noindex,follow` 非核心工具页改为短而准确的资料页，只保留工具自身用途、与类别匹配的注意事项、官方入口和同类入口，不再虚构“输入处理输出”“迁移团队成员”等通用工作流。
- 非核心工具缺少专属 `seedNote` 时，注意事项改为按 15 个类别生成的安全提醒，并带入具体工具名，减少跨类别语义错配与整段复用。
- 首页“常用入口”改为 15 个分类各 1 个代表工具，修复此前实际只覆盖前 5 个分类的问题。
- 核心 50 页移除重复的自动“相关推荐引导句”，保留每页自己的比较文案和相关工具卡片。
- GA4 改为访客明确同意后才动态加载；拒绝统计不影响站内功能，隐私政策同步更新。
- 自动检查增加首页 15 类代表入口、静态 GA4 禁止加载、旧万能工作流禁用等规则；短资料页不再用强制长文阈值鼓励凑字数。
- 仍保持 50 个核心工具页可索引、250 个非核心工具页 noindex，sitemap 共 70 个 URL。

# v1.3.0

- 50 个可索引核心工具页改为逐工具内容配置，不再通过正则猜测 SEO 标题或把通用操作流程套到所有工具。
- 每个核心页新增独立 SEO Title、H1、主关键词/次关键词规划、开场、使用场景、开始方式、注意事项和同类比较文案。
- 修复 Zoom、Buffer、Pexels、Pixabay、Vercel、Postman、Google Workspace、Microsoft 365、Search Console 等页面的标题或正文语义错配。
- 核心页不再出现“输入→处理→输出”“迁移团队成员”等万能流程句；自动检查会阻止旧模板句重新进入可索引页面。
- 分类页中英文名称之间的排版统一，例如“数据与 SEO 工具导航”“开发与 API 工具导航”。
- 结构化数据继续使用保守的 WebPage + Thing 描述，避免把素材库、字体网站等错误标成 SoftwareApplication。
- 250 个资料不足的工具页继续 `noindex,follow`，不进入 sitemap；核心 50 页与站点基础页面合计 70 个可索引 URL。

# v1.2.0

- 工具详情页不再从分类任务池硬套具体功能，避免 Canva / Search Console 一类语义错配。
- 50 个已有独立用途与使用提醒的工具页保持可索引；其余 250 个工具页保留导航访问并设为 `noindex,follow`，待补充专属资料后再开放索引。
- Sitemap 仅包含当前可索引页面，共 70 个 URL，并移除缺少可靠逐页更新时间依据的统一 `lastmod`。
- 404 页面改为 `noindex,follow`。
- 标题改为根据工具用途生成，减少分类级统一模板；正文采用多组自然结构，并取消分类任务轮换造成的事实错配。
- 统一清理重复标点，并加强自动检查：noindex 数量、Sitemap 数量、长句复用、内部说明外露和错误标点都会拦截构建检查。

# 迈引导航 v1.0.0 交付说明

## 本版内容

- 300 个工具介绍页，覆盖 15 个分类。
- 首页、全部工具页、15 个分类页、关于、隐私政策、使用条款和 404。
- 每个首页工具入口先进入本站介绍页，再由用户点击按钮前往官方网站。
- 本地搜索、浏览器本地收藏、移动端菜单和相关工具内链。
- GA4 `G-EEKYMLMF2H` 及固定参数的工具跳转、收藏和推广点击事件。
- 虎跃加速桌面与移动端静态图片横幅，落地到 `https://huyuejsq.co/`。
- canonical、Open Graph、JSON-LD、`sitemap.xml`、`robots.txt` 和 `llms.txt`。

## 自动检查结果

- HTML：321 个，其中 300 个工具页。
- sitemap：320 条可索引 URL，404 不进入 sitemap。
- 页面标题：321 个且全部唯一。
- H1：321 个且全部唯一。
- 工具正文最高相似度：0.317，检查阈值为 0.62。
- 内链、静态资源、JSON-LD、GA4 数量、推广链接属性和官方入口属性检查通过。

## 状态边界

本文件记录的是源码包状态，不代表 GitHub 已上传、Pages 已部署、GA4 已收到数据或搜索引擎已经收录。当前执行环境无法取得浏览器运行文件，未完成真实浏览器截图和像素级移动端检查；源码上传后仍需按 `DEPLOY_CHECKLIST.md` 做线上验收。

## 1.1.0 - 2026-09-21

- 重写工具详情页生成逻辑，优先使用每个工具自己的用途与注意事项数据，减少批量模板句。
- 按工具分类调整标题与描述，更贴近中文搜索意图。
- 工具页 JSON-LD 增加 `dateModified` 与官方网站实体引用；首页补充 Organization 实体。
- sitemap 的 `lastmod` 改为读取工具数据中的核验日期，不再在模板中为所有页面写死同一日期。
- 质量检查增加长句跨页面复用检测，防止后续内容再次模板化。
- 推广图片和 Open Graph 图片改为随源码保存，构建不再依赖特定工作目录或 Inkscape。
